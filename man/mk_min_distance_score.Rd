% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_min_distance.R
\name{mk_min_distance_score}
\alias{mk_min_distance_score}
\title{Create a scoring function that penalizes small distances between
samples of the same category}
\usage{
mk_min_distance_score(feature_var, batch_var, penalty_threshold = NULL)
}
\arguments{
\item{feature_var}{Name of the column in the batch container samples
that contains the feature/category to space out.}

\item{batch_var}{Name of the column in the batch container that defines
the position/order of samples (e.g., processing order, time point).}

\item{penalty_threshold}{Distances smaller than this value will be
penalized. If \code{NULL} (default), it is set to
\code{floor(n_locations / n_levels)}, which is the ideal spacing for even
distribution.}
}
\value{
A scoring function that takes a \link{BatchContainer} and returns
a numeric score (lower is better).
}
\description{
This scoring function is useful for ensuring even spacing of sample
categories along a one-dimensional layout, such as sample processing order.
It penalizes cases where samples of the same category are closer together
than the penalty threshold.
}
\details{
The penalty for each pair of adjacent same-category samples is
\code{(penalty_threshold - actual_distance)^2} when
\code{actual_distance < penalty_threshold}, and 0 otherwise. The total score is
the sum of all penalties.
}
\examples{
set.seed(42)
samples <- data.frame(
  sample_id = 1:40,
  treatment = rep(paste0("Trt", 1:4), each = 10),
  sex = rep(c("F", "M"), 20)
)

bc <- BatchContainer$new(
  dimensions = list(position = 40)
)
bc <- assign_random(bc, samples)

# Create scoring functions
scoring <- list(
  treatment = mk_min_distance_score("treatment", "position"),
  sex = mk_min_distance_score("sex", "position")
)

# Optimize
bc <- optimize_design(
  bc,
  scoring = scoring,
  aggregate_scores_func = sum_scores,
  max_iter = 500,
  quiet = TRUE
)
}
